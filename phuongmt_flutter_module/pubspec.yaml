name: phuongmt_flutter_module
description: A new flutter module project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/

  module:
    androidX: true
    androidPackage: com.phuongmt.phuongmt_flutter_module
    iosBundleIdentifier: com.phuongmt.phuongmtFlutterModule
